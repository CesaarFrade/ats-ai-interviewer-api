spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ats_database
    username: ats_user
    password: ats_super_secreto_123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true